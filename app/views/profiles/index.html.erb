<div class="flex justify-between">
  <%= render layout: 'shared/options', locals: { page_header: true } do %>
    <%= link_to t('create_new_profile'), new_profile_path,class: 'text-gray-700 block px-4 py-2 text-sm', role:'menuitem', tabindex:'-1' %>
  <% end %>
  <%= render 'search', url: profiles_path%>
</div>
<ul>
  <% if @profiles.count.zero? %>
    <p> No Profiles found</p>
  <% else %>
    <% @profiles.each_with_index do |profile, index| %>
      <li>
        <%= render 'card', profile: profile %>
      </li>
    <% end %>
  <% end %>
</ul>
